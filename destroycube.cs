using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class destroycube : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {   //this will destroy the cube
        Destroy(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
